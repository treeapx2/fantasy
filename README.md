# Fantasy Football Draft Prep App

Compiles multi-source rankings/analysis by player, supports personal rank/tier overrides
and risk/upside notes persisted across sessions, and (later) a draft-strategy layer and
an in-season lineup management tool.

## Status

**Phase 1 complete:** UDK mechanical extraction → canonical dataset.
**Phase 2 complete:** 20 supplemental UDK reports ingested into per-report sources.
**Phase 3 complete:** derived metrics layer on every canonical player.
**Not yet built:** refresh harness (6), notes synthesis (5), tag icons (4), app UI,
ESPN comparison layer, draft strategy model, in-season tool.

See `BUILD_SPEC.md` for the phase definitions.

## Repo layout

```
data/
  sources/
    udk/
      players_raw.json     UDK rank/tier/bio/ADP/projection columns, as extracted. Mirrors
                            the source layout — do not hand-edit; re-run build/extract_udk.py.
      field_mapping.json   udk raw field -> canonical field. Add an entry here before a new
                            raw field can flow into data/canonical/players.json.
    udk_consistency_charts/  Per-year end-of-season positional finish rank, 2016-2025.
    udk_consistency_pct/     Floor/ceiling/bust rates over the last 3 seasons, plus the
                              GP sample size those rates are computed on.
    udk_market_share/        Share of team production. RB rows carry both a rushing and a
                              receiving block; there is no QB file for this report.
    udk_red_zone/            Inside-20 and inside-10 opportunity. Split by play type, so a
                              player may carry passing, rushing and/or receiving blocks.
    udk_value_scout/         TrueValue vs market ADP, top 250. The volatile tier (Phase 6).
    _team/                 TEAM-grain sources. These carry team_raw.json rather than
                            players_raw.json and are deliberately skipped by
                            build_canonical.py — they do not join on player_id.
      udk_sos/               Strength of schedule by position, 18 weeks x 32 teams x 4 pos.
      udk_target_share/      Team target/completion split across WR, RB, TE.
    espn/                  (not yet created — Phase 4)
  canonical/
    players.json           UI-facing merged dataset. Generated by build/build_canonical.py.
                            Never hand-edit — it's fully regenerated on every build.
  user/
    my_ranks.json           Your rank/tier overrides, keyed by player_id. Independent of
                             any source's rank — never overwritten by a rebuild.
    notes_staging.json      Your raw dropped-in notes, unsorted, timestamped. You add to
                             this; on each update pass I sort entries into risk_upside_notes.json.
    risk_upside_notes.json  Per player: {risk: {udk: [], espn: [], user: []}, upside: {same}}.
                             Your notes stay in their own "user" sub-list — never merged into
                             or indistinguishable from source-derived notes.
build/
  extract_udk.py      Source of truth for UDK's raw data — the numbers live here as Python
                       tuples (versioned, diffable), not hand-typed JSON. Re-run to regenerate
                       data/sources/udk/players_raw.json.
  ingest_supplemental.py  One-shot ingest of the 20 staged UDK CSVs into the udk_* and
                       _team/ sources above. Validates every file's row count and exact
                       header signature before writing anything. The CSVs it consumed live
                       in git history (commit b14c8f4); re-running it without them aborts
                       cleanly.
  build_canonical.py  Merges all data/sources/*/players_raw.json into data/canonical/players.json
                       via each source's field_mapping.json. Reports unmapped fields rather than
                       silently dropping them. Also scaffolds (never overwrites) data/user/*.
  derive_metrics.py   Adds the `derived` block to each canonical player. Must run AFTER
                       build_canonical.py, which regenerates players.json and drops the block
                       by design. Idempotent.
```

## Adding a new source (e.g. ESPN) or a new field

1. Extract the source's raw data into `data/sources/<source>/players_raw.json` (same shape:
   `{source, source_label, source_date, players: [...]}`, each player carrying whatever
   fields that source has).
2. Write `data/sources/<source>/field_mapping.json` mapping that source's raw field names to
   canonical names (reuse an existing canonical name where it's the same underlying stat,
   e.g. both UDK and ESPN's "bye week" should map to canonical `bye`).
3. Run `python3 build/build_canonical.py`. Any raw field not yet in the mapping is printed
   as **unmapped** — it exists in the raw data but won't reach the UI until you add the
   mapping entry and, separately, add UI support for it. Two deliberate steps, nothing
   silent.

## player_id / cross-source name matching

`player_id` is a slug of `name-team-position`, minted from the **spine** source (`udk`,
the ranked draft board). Only the spine creates players; every other source may only
*attach* to a player the spine already knows. A supplemental row matching nobody falls out
and is reported — it is never promoted into the dataset. Without this the 304-WR
consistency report would inflate the board with players UDK does not rank.

**Supplemental sources match on name + position, not on `player_id`.** The slug embeds
team, and team is not stable across these sources: the 2026 ranking board lists players on
their 2026 teams while the historical stat reports list the team they produced those stats
for (A.J. Brown ranks as NE, has his market share under PHI). ~50 players per source
differ this way, so a team-bearing key would silently drop every offseason mover.

Names are folded accent- and punctuation-insensitively, which is what resolves
`Audric Estimé` → `Audric Estime`. `NAME_ALIASES` in `build/build_canonical.py` is for
genuine spelling differences folding cannot reach (e.g. ESPN's "Kenneth Walker III" vs
UDK's "Kenneth Walker"), and grows as mismatches are found. A name-only match (the Red
Zone reports carry no position) that is ambiguous is refused rather than guessed.

## Known data gaps in the current UDK extraction

- **Tag icons** (My Guy / Value / Sleeper / Breakout / Bust / Injury Concerns): the PDF
  renders these as small glyphs that don't survive text extraction reliably across 312
  rows. Only **Rookie** is auto-tagged (derived from `exp == 0`, which is unambiguous).
  The others are left as an empty `tags` array for now. If you want them, the honest fix is
  a dedicated visual re-pass over the PDF pages rather than a guess.
- **TE games/finish_2025**: cropped out of the source page layout for all 54 TEs — null for
  every TE record, not a sampling gap.
- **Rashee Shaheed (WR #54)**: source PDF lists `games: 18`, which isn't possible in an NFL
  season (max 17). Kept as-is from the source with this flag rather than silently "corrected"
  — worth a gut-check against the actual PDF if it matters for your notes.
- **Najee Harris (RB, NYG) is missing from the ranked 312.** He appears in UDK's own
  Value Scout at TrueValue 17.10 / ADP 19.04, so UDK considers him draftable, but he has
  no row in the rankings extraction — and the RB ranks run 1-91 with no gap, so he was not
  merely mis-numbered. He is the *only* one of Value Scout's 250 that fails to match. This
  looks like a gap in the Phase 1 hand-transcription, not a name-matching bug. Fixing it
  means adding his row to `build/extract_udk.py` from the PDF — do not hand-edit the
  generated JSON.
- **Josh Jacobs adp_edge is -202 picks** (UDK rank RB28, projection 180.6, but TrueValue
  round 21 against a round-4 ADP). This comes from UDK's own `Diff` column, not from our
  arithmetic — the source disagrees with itself. Matthew Stafford (-70) is the next
  largest. Worth an eyeball against the live site before acting on either.
- **Risk/upside notes**: not yet synthesized. `data/user/risk_upside_notes.json` is scaffolded
  with empty lists for every player, ready for the next phase.

## Derived metrics (Phase 3)

`build/derive_metrics.py` turns raw source columns — which are not comparable across
players — into ones that are, under `players[].derived`. **There is deliberately no
composite score.** The dimensions stay separate and visible; the weighing is yours.

Build order matters:

```bash
python3 build/extract_udk.py && python3 build/build_canonical.py && python3 build/derive_metrics.py
```

### Sample size is not optional

`floor_rate` / `ceiling_rate` / `bust_rate` are rates over a 3-season window whose length
ranges from 8 games to 51. `sample_confidence` (`high` >=34 GP, `medium` 17-33, `low` <17)
is emitted alongside them for every player that has them, and a gate refuses to write the
file if any player carries a rate without it. **Any UI that sorts on these must show it.**

The live example: Cam Skattebo posts an 87.5% Top-24 rate on **8 GP**; Jonathan Taylor
posts 80.5% on **41**. A naive sort puts Skattebo first. `derive_metrics.py` prints every
player with a >=70% floor rate on a low sample at the end of each run, precisely so this
cannot go unnoticed.

### Reading the metrics

- `trajectory_3yr` — least-squares slope of positional finish rank over the last 3 *played*
  seasons, x = the real year. **Negative = improving.** Null under 3 seasons. A 3-point
  slope is blind to a spike: CMC's 1 -> 72 -> 1 reads as a flat 0.0. `finish_volatility`
  (31.88 for him) is what catches it. Read them together.
- `peak_finish` / `years_since_peak` — best finish in the whole series; ties credit the most
  recent year.
- `opportunity_share` — RB = rush attempt share + target share; WR/TE = target share. QBs
  have no value: UDK publishes no QB market-share file.
- `td_dependency` — TD share minus yardage share for the position-appropriate block. Large
  positive = TD-dependent, i.e. regression exposure. `td_dependency_rush` and
  `_rec` stay exposed separately so a receiving back is not misread.
- `rz_volume_i10` — the player's own inside-10 scoring touches (carries + targets).
- `adp_edge` — see below.

### adp_edge is on a 12-team board

Value Scout's ADP is **12-team**, while the league is 10-team. Verified at ingest: pick
numbers in the source run to 12, and under 12-team pick math `(ADP - TrueValue)` reproduces
the source's own `Diff` column for all 246 rows carrying both values.

Two consequences:

1. `adp_edge` is `Average ADP - TrueValue`, **positive = market discount** — the sign
   follows the source's `Diff`. (BUILD_SPEC Phase 3 writes the formula as
   `TrueValue - Average ADP`, which contradicts its own "positive = market discount" label
   and matches `Diff` for only 18 of 246 rows. The source's convention wins.)
2. A "full round" of drift is **12 picks, not 10** — relevant to the Phase 6 change report.

`adp_edge` is null where either value is `Undrafted` in the source (4 players);
`adp_edge_status` says why. Edges on very deep players are noise in a 10-team league —
only ~160 picks happen — so filter by `avg_adp_pick` before ranking on it.

## Validation

All three build scripts are safe to re-run any time and are deterministic — a rebuild on an
unchanged tree produces byte-identical output, so `git status` staying clean after a rebuild
is itself a validation. After any change, a quick sanity check:

```python
import json
d = json.load(open("data/canonical/players.json"))
assert d["player_count"] == 312  # 36 QB + 91 RB + 131 WR + 54 TE as of the 8/30/2026 UDK
```

## GitHub

Lives at `https://github.com/treeapx2/fantasy` on branch `main`.

All file changes happen in-repo via Claude Code, committed and pushed from here. Artifacts
from a chat session are reproduced by a script in-repo — never hand-placed by extracting a
zip over the working tree, which is what caused the duplicate-directory mess Phase 0 had
to reconcile.
